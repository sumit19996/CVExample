<% content_for :head do %>
<title>Employee List</title>
<% end %>
<h1>Employee Model</h1>
<%= image_tag "emp1.gif",size: "647x485", alt: "Employees GIF" %>
<! -- <%= video_tag "abc.mp4",size: "647x485", controls: true %> 
<br>
<h3>Details of all the Employees</h3>
<table style="box-shadow: 1px 1px 15px 2px black;width: 80%">
  <tr>
    <th>Employee ID</th>
    <th>Name</th>
    <th>Designation</th>
    <th>Salary</th>
    <th colspan="3">Links</th>
  </tr>

  <% @employees.each do |emp| %>
  <tr>
    <td><%= emp.id %></td>
    <td><%= emp.name %></td>
    <td><%= emp.designation %></td>
    <td><%= emp.salary %></td>
    <td><%= link_to "Show", emp %></td>
    <td><%= link_to "Edit", edit_employee_path(emp) %></td>
    <td><%= link_to 'Destroy', employee_path(emp), method: :delete,data: { confirm: 'Are you sure?' } %></td>  
   <!-- <td><%= check_box("Employee", emp.id) %></td>
    <td>  <%= file_field(:user, :avatar) %></td>
    <td>  <%= radio_button("article", "category", "rails") %></td>
      <td><%= label(:employee, :sumit) %></td>
      <td><%= password_field(:login, :pass) %></td> -->
    </tr>
    <% end %>
</table>
<br>
<%= submit_tag "Publish this article" %>
<h2><%= link_to "New Employee", new_employee_path %></h2>
<!-- <%= label_tag(:chk, "Checked") %>  
<%= text_field_tag(:comment) %> -->
<p><%= @text %>
